{
  "name": "api-testing",
  "version": "1.0.0",
  "description": "Proyecto para testear APIs con Vitest",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "vitest run --reporter=default --reporter=junit --outputFile.junit=reports/junit.xml"
  },
  "devDependencies": {
    "vitest": "^5.0.1",
    "zod": "^4.6.5"
  }
}
